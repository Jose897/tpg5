/*
 *
 *
 *
 *
 *
 */
#include <iostream>

using namespace std;

template <typename Peso>
class Etiqueta{

	private:
		Peso acumulador;
		string procedencia;
		string cod;
		int cant_ady;
		int iteracion;


	public:


template <typename Peso>
Etiqueta::Etiqueta(){
}

template <typename Peso>
Etiqueta::Etiqueta(){
}

template <typename Peso>
Etiqueta::Etiqueta(){
}
template <typename Peso>
Etiqueta::Etiqueta(){
}

template <typename Peso>
Etiqueta::Etiqueta(){
}
};
